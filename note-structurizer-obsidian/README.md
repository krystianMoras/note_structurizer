# Helper plugin

# Installation

```
npm run package
cp -r dist/note-structurizer-obsidian <your_vault>/.obsidian/plugins
```

## Script setup

1. Put the enforcer script in your vault
2. If you don't use global env set the project path like this:
```
cd ~/Documents/projects/note_structure_enforcer && /opt/homebrew/bin/uv run
```
3. Run the script in the current file with Cmd+P -> `<script name>`


# Special thanks

Obsidian team is doing a really good job at finally solving the note taking apps. Obsidian is free but you can donate to them: https://obsidian.md/pricing
